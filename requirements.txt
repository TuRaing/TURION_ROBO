# TURION — Python dependencies

anthropic          # Claude API
openai-whisper     # Speech-to-Text (local, English)
transformers       # Speech-to-Text (local, Marathi/Indic — AI4Bharat IndicConformer)
torchaudio
onnx
onnxruntime
sounddevice        # mic capture
pyttsx3            # Text-to-Speech (English)
piper-tts          # Text-to-Speech (local, Marathi/Hindi — Piper voices)
openwakeword       # Wake-word detection ("Hi Sisu", custom-trained — see Doc/project_info.md)
jyotishganit       # Hindu Panchanga (tithi/nakshatra) — given to Claude alongside today's date
pywebview          # Desktop app window (turion/gui/) — HTML/CSS UI instead of the terminal
opencv-python      # Camera frame decoding (turion/vision/) — Phase 2
ultralytics        # Object detection, YOLO-World (local, free) — turion/vision/object_detection.py
insightface        # Face detection (local, free, onnxruntime-based) — turion/vision/face_detection.py
